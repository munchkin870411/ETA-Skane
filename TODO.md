ShareText.io
Text Share
QR Generator
URL Shortener
Create New Share

## Repo settings (DONE)

- Block direct push to `master` / require PRs **(high)**
- Require CI checks before merge **(high)**
- Require 1 approval **(med)**
- Enable squash merge **(med)**
- Auto-delete merged branches **(low)**
- Rename `master` → `main` later **(low)**

## Security (DONE)

- Enable Dependabot alerts **(high)**
- Enable Dependabot security updates **(high)**

## CI / Automation (DONE)

- Add GitHub Actions CI (`lint`, `typecheck`, `build`) **(high)**
- Run CI on PR open / sync / reopen + push to default branch **(high)**
- Add PR template **(low)**

## Dependency management

- Add Renovate bot for dependency updates **(med)**

## Codebase (DONE)

- Add Prettier **(high)** ✓
- Add scripts: `typecheck`, `format`, `format:check` **(high)** ✓
- Improve `lint` script to explicitly target repo files **(med)** ✓
- Add Husky + lint-staged for pre-commit checks **(med)** ✓
- Consider disabling `allowJs` in `tsconfig` (TS-first repo) **(med)** ✓

## Website / Product quality (DONE)

- Fix site metadata (description, title, etc.) **(high)** ✓
  - NOTE: Update `metadataBase` URL in `app/layout.tsx` when final domain is confirmed
  - NOTE: Customize descriptions to better match specific services
- Add `robots.ts` and `sitemap.ts` **(med)** ✓
- Add Open Graph / social metadata **(med)** ✓

## Documentation (DONE)

- Replace default README with setup + scripts documentation **(med)** ✓

## Structure / Maintainability

- Keep reusable UI in `components/ui` and sections in `components` **(low)**
- Introduce `src/` structure only if the project grows **(low)**

Create New Share
✨ AI Blog Post Writer
Create standout articles in seconds with RightBlogger. Fast. Easy. Loved by readers.
Share Text
QR Code Generator
URL Shortener
Chrome Extension
Privacy Policy
Created by Andy Feliciotti
Enjoying? Buy me a coffee
389.2K shares created
ShareText is not responsible for the contents of shared links. Please use the report button to notify us of any inappropriate content.
